<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Sh-sh-sh-Shopping</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="/style.css">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
</head>
<body>

  <nav role="navigation" class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Sh-sh-sh-Shopping</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="/about">About</a>
        </li>
        {{#if login}}
        <li class="nav-item">
          <a class="nav-link" href="/shoppinglist">My Shopping Lists</a>
        </li>
        {{/if}}
      </ul>
      {{#unless login}}
      <form id="loginNav" action="/login" method="GET" class="form-inline my-2 my-lg-0">
        <button id="loginButton" class="btn btn-primary my-2 my-sm-0" type="submit" value="login">Login</button>
      </form>
      {{/unless}}
      {{#unless login}}
      <form id="registerNav" action="/register" method="GET" class="form-inline my-2 my-lg-0">
        <button id="registerButton" class="btn btn-warning my-2 my-sm-0" type="submit" value="register">Register</button>
      </form>
      {{/unless}}
      {{#if login}}
      <form id="adminNav" action="/admin-portal" method="GET" class="form-inline my-2 my-lg-0">
        <button id="adminButton" class="btn btn-info my-2 my-sm-0" type="submit" value="admin">For Admins</button>
      </form>
      {{/if}}
      {{#if login}}
      <form id="logoutNav" action="/logout" method="GET" class="form-inline my-2 my-lg-0">
        <button id="logoutButton" class="btn btn-danger my-2 my-sm-0" type="submit" value="logout">Logout</button>
      </form>
      {{/if}}
    </div>
  </nav>

  {{{body}}}

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
  <script src="/script.js"></script>
</body>
</html>
